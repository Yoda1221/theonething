const DEFAULTVALUES = Object.freeze({
    focus: 30,
    relax: 10,
    sentence: ""
})

const MODE = Object.freeze({
    focus: "focus",
    relax: "relax"
})

export { 
    DEFAULTVALUES,
    MODE 
}
